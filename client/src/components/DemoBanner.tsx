const styles: React.CSSProperties = {
  backgroundColor: '#1e40af',
  color: 'white',
  padding: '0.5rem 1rem',
  textAlign: 'center',
  fontSize: '0.875rem',
}

export default function DemoBanner() {
  return <div style={styles}>Demo mode — no data is saved</div>
}
