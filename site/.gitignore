# Hugo build output
public/
.hugo_build.lock

# Hugo resource cache (SCSS compilation artifacts, etc.)
resources/

# Hugo vendored modules (fetched at build time via go.mod/go.sum)
_vendor/

# Generated demo SPA (built by site-build-with-demo; not checked in)
static/demo/
